apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-cicd-pvc
  namespace: hk-jenkins
spec:
  storageClassName: hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-ops-pvc
  namespace: hk-jenkins
spec:
  storageClassName: hostpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
